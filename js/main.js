//tab-navaigator = tabs
//link = link
//content = tab

class Link{
    constructor(link){
        this.link = link;
        this.
    }
}

const links = document.querySelectorAll('.link')

links.forEach(link =>{
    new link(link);
    this.tab =
})